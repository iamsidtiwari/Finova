# Frontend Environment Variables (Vite)
# These are bundled into the frontend build and are NOT secrets

# Backend API URL - Change this to your Railway backend URL for production
VITE_API_URL=http://localhost:5000/api

# Socket.IO URL - Change this to your Railway backend URL for production
VITE_SOCKET_URL=http://localhost:5000
